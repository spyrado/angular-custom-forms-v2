import { EnableOnCheck1Directive } from './enable-on-check1.directive';

describe('EnableOnCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new EnableOnCheck1Directive();
    expect(directive).toBeTruthy();
  });
});
