import { EnableOnCheck2Directive } from './enable-on-check2.directive';

describe('EnableOnCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new EnableOnCheck2Directive();
    expect(directive).toBeTruthy();
  });
});
